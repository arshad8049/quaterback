# Option A — hosted proxy (recommended for beta users, no API key needed)
# QB_PROXY_URL=https://your-site.netlify.app/api
# QB_PROXY_SECRET=optional-shared-secret   # must match Netlify env var

# Option B — bring your own Anthropic API key
# ANTHROPIC_API_KEY=sk-ant-...
